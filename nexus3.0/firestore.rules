rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Client SDKs only. firebase-admin bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
